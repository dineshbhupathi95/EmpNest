import React from 'react';
import { Box, Typography } from '@mui/material';

const Clients = () => {
  return (
    <Box>
      <Typography variant="h4">Clients</Typography>
      <Typography sx={{ mt: 2 }}>Manage your client information here.</Typography>
    </Box>
  );
};

export default Clients;
